[pytest]
addopts = -ra

markers =
    sanity: Quick smoke coverage for critical paths
    regression: Full regression coverage
    smoke: Very fast checks for CI gating
    unit: Isolated unit tests (no browser/network)
    e2e: Browser end-to-end tests
    streamlit: Streamlit app checks

python_files = test_*.py
